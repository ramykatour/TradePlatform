# Trading Platform Internal Exchange Roadmap

##  التداول الداخلي على المنصة

---

## المرحلة الحالية (قبل الترخيص)

- الباكند مكتمل: Spot Trading + Futures Trading + Deposit + Withdrawal + Wallet + Admin Panel
- التداول يتم عن طريق KuCoin API (مؤقت للاختبار فقط)
- الفرونت إند الحالي هو prototype للاختبار فقط — سيتم إعادة بناؤه بالكامل
- نظام الإيداع: يوزر واحد = عنوان واحد مستقل (سيتم تطبيقه بعد الترخيص)

---

## بعد الترخيص — الهدف

التداول يتم داخل المنصة بالكامل.  
اليوزرات يتداولوا مع بعض مباشرة بدون الاعتماد على KuCoin.

---

## المكونات المطلوبة

### 1. Matching Engine — قلب المنصة

أصعب جزء في المشروع.  
مهمته: مطابقة أوامر الشراء مع أوامر البيع بالسعر والكمية.

- يُكتب بـ C++ أو Rust لضمان السرعة
- يعمل آلاف العمليات في الثانية
- يجب أن يكون consistent — لا يضيع أوردر واحد
- يدعم أنواع الأوردرات: Market, Limit, Stop-Limit

---

### 2. Order Book

- يحفظ كل الأوردرات المفتوحة (Bids / Asks)
- يُحدَّث في real-time عند كل صفقة
- يعرض للمستخدمين عبر WebSocket

---

### 3. Liquidity — التحدي الأكبر

**المشكلة:**  
في البداية عدد المستخدمين قليل، فالـ Order Book يكون فارغاً.  
مثال: يوزر A يريد شراء BTC بـ 50,000$ — يوزر B يريد البيع بـ 52,000$ — لا توجد صفقة.

**الحل — Hybrid Model:**

```
يوزر يعمل Order على المنصة
            ↓
هل يوجد match مع يوزر آخر داخلياً؟
            ↓
نعم → ينفذ الأوردر داخلياً (أفضل)
لا  → يُرسل لـ KuCoin أو Liquidity Provider لتنفيذه (Hedge)
```

كده المنصة عندها سيولة من أول يوم.  
مع زيادة المستخدمين يقل الاعتماد على الـ Hedge تدريجياً.

---

### 4. Settlement Engine

- يُسجل نتيجة كل صفقة في الـ Database
- يُحدِّث أرصدة المشترين والبائعين فوراً
- يُنشئ سجل تاريخي لكل عملية

---

### 5. Risk Management

- منع الأوردرات اللي تتجاوز رصيد المستخدم
- حماية من التلاعب بالأسعار
- Circuit Breaker: إيقاف التداول تلقائياً لو الأسعار تحركت بشكل غير طبيعي

---

## الملفات الجديدة المطلوبة (باكند)

| الملف | الوظيفة |
|---|---|
| `matchingEngine.js` أو `matching_engine.rs` | مطابقة الأوردرات |
| `orderBookService.js` | إدارة الـ Order Book |
| `settlementService.js` | تسوية الصفقات وتحديث الأرصدة |
| `hedgeService.js` | إرسال الأوردرات لـ KuCoin لو مفيش match |
| `riskService.js` | فحص الرصيد والمخاطر قبل تنفيذ الأوردر |
| `priceService.js` | جلب الأسعار المرجعية من السوق |

---

## الملفات التي ستتغير (باكند)

| الملف | التغيير |
|---|---|
| `tradeController.js` | يتوقف عن استخدام KuCoin ويستخدم Matching Engine |
| `wsServer.js` | يبث تحديثات الـ Order Book للمستخدمين |
| `depositController.js` | يربط بنظام المحافظ الجديد |
| `txMonitor.js` | يراقب البلوكشين مباشرة |

---

## الملفات التي لن تتغير

| الملف | السبب |
|---|---|
| `walletService.js` | منطق الرصيد نفسه |
| `withdrawalController.js` | السحب نفسه |
| `adminController.js` | الأدمن نفسه |
| `authController.js` | المصادقة نفسها |
| `database.js` | قاعدة البيانات نفسها |

---

## الفرونت إند — إعادة بناء كاملة

الفرونت إند الحالي مؤقت للاختبار فقط.  
بعد الترخيص سيُعاد بناؤه بالكامل ليشمل:

- صفحة التداول: Order Book + Chart + نموذج الشراء والبيع
- Real-time updates عبر WebSocket
- صفحة الإيداع: عنوان مستقل لكل مستخدم مع QR Code
- صفحة السحب
- صفحة الـ History
- لوحة الأدمن الكاملة

---

## الجدول الزمني التقريبي (بعد الترخيص)

| المرحلة | المدة | التفاصيل |
|---|---|---|
| نظام الإيداع (محافظ منفصلة) | 1- اسبوع| CoinsPaid أو HD Wallet |
| Matching Engine (أساسي) | 2-3 أسابيع | Limit + Market Orders |
| Order Book + WebSocket | 3-4 أسابيع | Real-time |
| Settlement + Risk | 2 أسابيع | تسوية وحماية |
| Hybrid Liquidity (KuCoin Hedge) | 2-3 أسابيع | للسيولة في البداية |
| إعادة بناء الفرونت إند | 1 اسبوع | تصميم كامل |
| اختبار وإطلاق | شهر | QA + Security Audit |


---

## ملاحظات مهمة


1. **Security Audit إلزامي** — قبل الإطلاق لازم شركة متخصصة تراجع الكود
2. **الـ Matching Engine هو الأهم** — لو فيه bug واحد ممكن يخسر اليوزرات فلوس


---


